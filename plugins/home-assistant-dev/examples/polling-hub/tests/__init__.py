"""Test suite for the polling-hub example integration.

Scope: this example ships config-flow tests (test_config_flow.py) only — it
demonstrates the config/options/reauth flow patterns, not full platform coverage.
The init/coordinator/sensor test modules referenced by the ha-testing skill are
intentionally not included here to keep the reference example focused; see that
skill for the patterns a production integration's own test suite should add.
"""
