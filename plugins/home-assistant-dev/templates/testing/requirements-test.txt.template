# Testing requirements for Home Assistant custom integrations
#
# Install with: pip install -r requirements-test.txt

# Core testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=4.1.0
pytest-timeout>=2.2.0

# Home Assistant testing utilities
# Pin this to match your integration's targeted HA core version — the package is
# version-locked to HA core, so an unbounded floor can resolve to a release
# incompatible with your HA baseline.
pytest-homeassistant-custom-component>=0.13.0

# Mocking
pytest-mock>=3.12.0

# Code quality (optional but recommended)
ruff>=0.11.0
mypy>=1.8.0

# Type stubs for mypy
types-python-dateutil
types-requests
