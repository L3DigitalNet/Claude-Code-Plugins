<!--
Reference template for manual / Claude-driven documentation authoring. This file is
NOT read by scripts/generate-docs.py, which generates README.md programmatically from
the integration code — edits here do not change generated output. generate-docs.py is
the source of truth for generated docs.

PLACEHOLDERS — find-and-replace every `{token}` below before shipping (a manual copier
who misses one ships the literal `{token}` string). The full set across all doc/testing
templates is: {name} {description} {domain} {features} {platforms} {min_ha_version}
{requirements} {github_user} {github_repo} {license} {configuration_options} {entities}
{troubleshooting} {integration_name}. Tokens used in THIS file:

  {name}                  — human-readable integration name (e.g. "My Integration")
  {description}           — one-line summary of what the integration does
  {domain}               — custom_components folder/domain slug (e.g. "my_integration")
  {features}             — Markdown bullet list of headline features
  {github_user}          — GitHub owner/org of the repo
  {github_repo}          — GitHub repository name
  {configuration_options} — Markdown describing config-flow / options-flow settings
  {entities}             — Markdown list/table of entities the integration creates
  {troubleshooting}      — Markdown troubleshooting section (common issues + fixes)
  {license}              — SPDX license id or name (e.g. "MIT")

When generate-docs.py fills a README it hardcodes some of these (License=MIT, a fixed
troubleshooting block) and ignores this template — so this manifest is the contract for
MANUAL copiers and for validating the generator's placeholder coverage.
-->

# {name}

[![hacs_badge](https://img.shields.io/badge/HACS-Custom-41BDF5.svg)](https://github.com/hacs/integration)
[![GitHub Release](https://img.shields.io/github/release/{github_user}/{github_repo}.svg)](https://github.com/{github_user}/{github_repo}/releases)

{description}

## Features

{features}

## Installation

### HACS (Recommended)

1. Open HACS in Home Assistant
2. Click "Integrations"
3. Click ⋮ → "Custom repositories"
4. Add `https://github.com/{github_user}/{github_repo}` as category "Integration"
5. Click "{name}" → "Download"
6. Restart Home Assistant

### Manual

1. Download the latest release from [GitHub](https://github.com/{github_user}/{github_repo}/releases)
2. Extract and copy `custom_components/{domain}` to your `custom_components` folder
3. Restart Home Assistant

## Configuration

1. Go to **Settings** → **Devices & Services**
2. Click **+ Add Integration**
3. Search for "{name}"
4. Follow the setup wizard

{configuration_options}

## Entities

{entities}

## Troubleshooting

{troubleshooting}

## Support

- [GitHub Issues](https://github.com/{github_user}/{github_repo}/issues)

## License

{license}
