# Sessions — 2026-07

| Date | Summary |
| --- | --- |
| 2026-07-01 | **spec-pipeline plugin — designed, planned, Codex-converged; implementation deferred to next session.** Brainstormed merging the two agent-configs skills `author-master-spec` (v1.6) + `autonomous-phase-execution` (v1.11) into one marketplace plugin. Locked decisions: name `spec-pipeline`; **validators only** (enforcement hooks deferred to v2); utility commands `/validate` `/status` `/init-project`; Codex/ultracode review deps stay hard-required; architecture = single stdlib-only `specpipe` CLI (Option A over per-concern scripts — cross-artifact citation validation needs one parser layer). Spec `docs/superpowers/specs/2026-07-01-spec-pipeline-plugin-design.md` + 14-task TDD plan `docs/superpowers/plans/2026-07-01-spec-pipeline-plugin.md`. **Spec Codex-converged 4 rounds** (SA-001 scope-coverage narrowed to review-only; SA-002 resume-first `next-phase` + `in_progress→pending` recovery; SA-003 evidence execution-safety contract — argv/no-shell, timeout, 64 KiB cap, whole-block redaction incl. cmd string; SA-004 `--expect-failure-regex` mandatory for generic RED; SA-005 `status --state` + upward search; SA-006 stale validate-marketplace claim fixed + `claude plugin validate --strict` gate; SA-NEW-001 dropped Python packaging entirely — plain package dir + `PYTHONPATH` + `uv run --no-project -B`; SA-NEW-002 ignored-artifact clean-tree proof). **Plan Codex-converged 2 rounds, r2 clean** (CR-001 positive pytest RED signature `N failed`/`FAILED` — non-zero exit alone never passes; CR-002 bytes-safe `TimeoutExpired` decode; CR-003 `--json` narrowed to query subcommands; CR-004 commit-after-green enforcement in `_tdd_ok`; CR-005 markdownlint `--fix` pass + explicit staging in the final gate). Plan kept synced to the spec after every round. Repo plumbing: `docs/codex-reviews/` exempted from markdownlint/prettier (generated audit evidence); 6 audits committed. Stale MEMORY.md claim corrected (validate-marketplace.sh DOES validate plugin.json; `--strict` CLI verified). Commits `1d64ddd..` this session, pushed. **Resume:** execute the plan (subagent-driven recommended), then live smoke + `/release-pipeline:release` 0.1.0, then decide deprecation of the two source skills in agent-configs. |
| 2026-07-02 | **spec-pipeline plugin implemented — 14/14 tasks, final review READY TO MERGE, pushed pending.** Executed `docs/superpowers/plans/2026-07-01-spec-pipeline-plugin.md` via `superpowers:subagent-driven-development`: fresh implementer subagent per task (Haiku for verbatim transcription, Sonnet for edit/judgment tasks), Sonnet task reviewer per task, Fable final whole-branch review. Commits `44ad1fd..4842a7f` (17) + `37d191a` (TODO). specpipe CLI complete (findings/grammar/phaseplan/specdoc/plandoc/evidence/rounds/scaffold; stdlib-only, plain package via `PYTHONPATH` + `uv run --no-project python -B`, **101 tests** via `tests/run_tests.sh`); 4 grammar-conformant templates; skills `author`+`execute-phase` v2.0 migrated with validator gates (sources in agent-configs untouched); commands `/validate` `/status` `/init-project`; README/CHANGELOG; marketplace entry (8th plugin). In-flight fixes: `9098382` README ultracore→ultracode (Task 13 review), `91806bf` markdownlint MD041/MD033 (Task 14 gate). **Final review found 2 Important defects in plan-authored code, both verified by live execution:** fence-unaware decision-citation scan → false SPEC-DANGLING-DECISION errors could wedge the gate loop; invalid `--expect-failure-regex` crashed with uncaught `re.error` instead of exit 2. Fixed + 2 real-bug Minors (set_status 0600 mode-tightening; rounds non-dict-JSON AttributeError) in `4842a7f` with 4 discriminating regression tests; re-review verdict READY TO MERGE. Plan quirks adjudicated: Task 12 Step 3(a) stale old-text (post-rename), test-count prose miscounts (18/≈60 vs real 17/97). All Task 14 gates green: suite, validate-marketplace, `claude plugin validate --strict`, prettier, markdownlint, AC4/5/9 (clean tree incl. ignored artifacts). **Resume:** live smoke (install + cache sync), `/release-pipeline:release` 0.1.0, deprecation decision for the two source skills; 0.1.x hygiene backlog in TODO.md. |
| 2026-07-02 | **spec-pipeline fable-reviewed; all 15 findings fixed RED-first; 122 tests; pushed.** `/fable-review spec-pipeline` read all 38 plugin files (2.7k lines), ran the suite, and execution-verified two edge cases before reporting: 0 Critical / 0 High / 4 Medium / 11 Low. Mediums: R-001 `classify()` checked "test" before "implement" so an implement step naming a test-ish symbol (`Implement run_tests helper`) false-errored PLAN-TDD-ORDER; R-002 GREEN accepted any exit-0 command (asymmetric with the hardened RED gate) — a typo'd command could forge the committed audit trail; R-003 `phaseplan.parse` was the only fence-unaware parser — a fenced example `## Phase 99` became a real phase and `next_phase` resolved the phantom; R-004 README claimed the skills HALT when `/codex-review`/ultracode are missing but neither skill listed that halt condition. All 15 fixed in `ec74a16` (18 files, +325/−56) with 21 RED-first regression tests (watched all fail, then implemented): GREEN now needs pytest `N passed` or `record-green --framework generic --expect-success-regex`; supplied regexes always enforced (never silently ignored under pytest); `parse()` fence-opaque with `status_line` recorded so `set_status` reuses one parse instead of a second scan; word-boundary `grammar.phrase_re()` + lookaround symbol matching; PLAN-EMPTY-SYMBOLS warning; `next-phase` JSON reason `all_complete`\|`blocked`; `_find_state` stops at the `.git` boundary; redact-before-cap; Python ≥3.11 startup guard (exit 2); `blocked→pending` transition + template note that `complete` is terminal; HALT preconditions added to both skills (v2.0→v2.1); PP-CYCLE test added. R-012 (collection-error phrase appearing as test DATA falsely rejects RED) documented in-code as an accepted conservative limitation rather than weakened. Gates green: 122/122, validate-marketplace, markdownlint (253 files, 0 errors), live CLI smoke. Word-boundary scans thereby cleared from the 0.1.x hygiene backlog. **Resume:** unchanged — live smoke in a fresh session, `/release-pipeline:release` 0.1.0, source-skill deprecation decision. |
| 2026-07-02 | **All 7 marketplace plugins released via two batch-release passes; ENV-001 refined; Bug 9 recorded.** Pass 1 (`/release-pipeline:release` → Plugin Release, then Batch Release for the remaining 6): released spec-pipeline v0.2.0 individually, then batch-released home-assistant-dev v2.2.11, qt-suite v0.3.4, qdev v2.0.3 clean; quarantined release-pipeline, up-docs, uv-strict-python on real Phase 1 test failures (not flaky — each root-caused). **release-pipeline** AB5 (`auto-build-plugins.bats`) failed because ENV-001's global `export PATH="/usr/bin:/bin:$PATH"` shadowed the test's `npm` stub with the real binary; same pattern independently broke **up-docs**'s `server-inspect.bats` SI2-SI4 via its `ssh` stub. Fixed both by scoping the guard to the python3 call site(s) instead of exporting it globally (`ba975a4`, `c0919f9`); also fixed 4 release-pipeline legacy test scripts missing `core.hooksPath /dev/null` (TEST-003). **uv-strict-python**'s `check-standard-sync.sh` failed on real drift — pins stale against `project-standards` (`79daeae`/`a14ac7d` → HEAD `6cf2228`) and two templates byte-diverged; re-synced (`364f723`): ruff floor `0.9.0`→`0.14` (py314 target-version support), dropped `pytest-cov` (coverage measured via `coverage.py` directly), added a narrow `BaseException` cleanup-and-re-raise carve-out to the coding standard. Swept the other 11 ENV-001-guarded scripts for the same exposure — all call only python3 downstream (or, for `uv-strict-python/tests/run.sh`, run as a top-level bats launcher where per-test `setup()` re-prepends stub dirs afterward) — none affected, no changes made. Pass 2 re-ran batch release for the 3 fixed plugins, all green: release-pipeline v2.2.3, up-docs v0.13.1, uv-strict-python v0.2.1. `detect-unreleased.sh` confirms zero plugins remain unreleased. Recorded **Bug 9** (`docs/handoff/bugs/009-...md`) and refined **ENV-001** in `conventions.md` with the scoping caveat. Noted (not fixed, non-blocking): a stale `.release-waivers.json` `noreply_email` waiver entry. Commits `3846a2b..dc7f502` this session, all pushed. **Resume:** spec-pipeline live smoke test + source-skill deprecation decision (agent-configs); `.release-waivers.json` cleanup; rest of TODO.md backlog. |
| 2026-07-09 | Migrated the dual Claude/Codex profile and canonical status/task paths to Agent Handoff v1 while preserving the user-owned task section. |
| 2026-07-10 | **Removed release-pipeline + test-driver plugins (all layers); housekeeping pass.** Both plugins fully deleted: repo files + `marketplace.json` entries, `.claude/settings.json` enable (release-pipeline), live Claude Code state (`installed_plugins.json`, cache, catalog clone), and **all git tags + GitHub releases** (release-pipeline 13+13, test-driver 7+7) local+remote — user chose deletion over the prior tag-retaining pattern. release-pipeline **was** the documented release mechanism, so its `/release-pipeline:release` references across README/CLAUDE/AGENTS/BRANCH_PROTECTION/copilot-instructions + handoff docs were rewritten to a manual flow (bump plugin.json + marketplace.json → commit → tag `<name>/vX.Y.Z` → push → `gh release create`). Deleted `.release-waivers.json` (orphaned release-pipeline pre-flight artifact) + its TODO item. Marketplace now 6 plugins. **Gotcha caught in verification:** `gh release list --limit N` truncates across ALL plugins — first pass left 8 orphaned release-pipeline releases (tags deleted, releases not); re-verified at `--limit 500`. Housekeeping (project-standards workflow): fixed `docs/handoff/bugs/_regen_index.py` to emit Prettier-style table delimiters so generated INDEX.md passes the gate; markdownlint/Prettier fix pass on 5 plugin CHANGELOGs + bug 009 + TODO.md (cosmetic). Both adopted gates (Prettier, markdownlint) green repo-wide; bug/specs-plans/research indexes confirmed current. **Then, at user direction, reversed the retention convention:** deleted 8 executed implementation plans (`docs/plans/*-plan.md` ×3, `docs/superpowers/plans/` ×5) + all 6 resolved spec-pipeline Codex reviews; kept specs/designs (the historical record) + research KB. Added **DOC-006** (plans & reviews are ephemeral; specs retained) and revised DOC-005 accordingly; tombstoned the 8 deleted-plan rows in `specs-plans.md` (kept their release/outcome); delinked a pre-existing dangling ref in bug 006. Repo-wide broken-link scan clean for the deletions. **Note (not fixed):** `plugins/qdev/scripts/build_research_index.py` emits non-Prettier frontmatter YAML — latent, surfaces only on regen; defer to a qdev release (tracked in TODO). Commits `69e26b5..HEAD`, pushed. **Resume:** spec-pipeline live smoke + source-skill deprecation decision. |
