# Sessions — 2026-05

| Date | Summary |
|---|---|
| 2026-05-08 | Plugin cleanup re-inventory. Following commit `3b8323e` (`Cleaning up old plugins.`) which deleted 5 plugin source directories (claude-sync, design-assistant, docs-manager, linux-sysadmin, python-dev), reconciled every doc layer that named plugins by hand: `marketplace.json` shrank 17 → 12 entries; `README.md` rewrote intro paragraph + 17-row plugin table → 12-row + dropped 5 detail sections + repo-structure tree; `docs/architecture.md` updated count + tree; `docs/deployed.md` dropped 3 rows (docs-manager, python-dev, linux-sysadmin) and added `Recently Removed` section; `docs/conventions.md` TEST-001 test-counts list trimmed (15 → 11 entries) and TEST-002 affected-plugins list trimmed (13 → 9); `docs/skills.md` removed entire `## Linux Sysadmin Skill Conventions` section (87 lines, lines 461-547) since the conventions only applied to a now-deleted plugin; `docs/plugin-marketplaces.md` example swapped design-assistant → release-pipeline; `docs/plugin-design-template-full.md` deleted broken pointer to `plugins/docs-manager/docs/design.md §3`; `testing/STRATEGY.md` rewrote §1 Scope (in-scope 15 → 11; only qdev now excluded for being pure-markdown), trimmed §3 legacy-runners table (claude-sync row dropped), trimmed §6 priority table (linux-sysadmin/claude-sync/docs-manager/design-assistant rows dropped), §10 doc index 14 → 11; deleted `testing/plans/{claude-sync,design-assistant,docs-manager,linux-sysadmin}.md`. `.serena/` was already in a deleted-but-unstaged working-tree state on entry — independent serena-tool cleanup, not touched by this session. Left untouched as historical artifacts: `docs/plans/`, `docs/superpowers/{plans,specs}/`, `docs/ux_refresh/`, `.release-waivers.json`, prior session logs. python-dev/qdev are pure-markdown plugins; both were originally testing-excluded — now python-dev is also gone, leaving qdev as the only marketplace exclusion. |
| 2026-05-07 | Marketplace-wide search-MCP migration + testing-branch retirement + 5 plugin releases. Search stack: replaced garylab Python `serper-mcp-server` (13 Google verticals via `uvx`) with marcopesani Node `serper-search-scrape-mcp-server` (2 tools: `google_search` + `scrape`); added official Tavily HTTP MCP (`tavily_search`, `tavily_extract`, `tavily_map`, `tavily_crawl`, `tavily_research`). Net 19 → 13 search-domain tools. Routing rule rewritten as a per-intent table (general/news/finance/content-heavy/JS-rendered/site-exploration/lost-verticals) and propagated to 6 layers: `~/.claude/CLAUDE.md`, `~/.codex/AGENTS.md`, `agent-configs/global/{claude,codex}/`, `agent-configs/skills/populate-config/`, `dotfiles/common/.claude/CLAUDE.md`. Plugin releases in pipeline order: plugin-test-harness 0.7.5 (test-fix for 4 fix/ tests via `core.hooksPath=/dev/null` in tmpdir setup; bug 005 + TEST-003), home-assistant-dev 2.2.8 (drop `WebSearch` from `VALID_AGENT_TOOLS`), docs-manager 0.2.4 (replace `WebSearch` with MCP search backends in 2 agents), qdev 1.4.0 (Tavily MCP support across all commands + agents), release-pipeline 2.2.0 (bootstrap release; templates simplified for direct-to-main). Branch retirement: `testing` deleted (local + remote), GitHub `lock_branch` protection on main removed, 14 `tests/*` branches deleted with content cherry-picked to main as 17 commits (~225 test cases preserved across docs-manager/handoff/github-repo-manager/repo-hygiene/linux-sysadmin/home-assistant-dev/claude-sync/opus-context/design-assistant/up-docs/qt-suite/nominal/test-driver — release-pipeline contributed 77 cases). Rule docs rewritten: `BRANCH_PROTECTION.md` (422 → 60 lines), `CLAUDE.md`, `AGENTS.md`, `README.md`, `.github/copilot-instructions.md`, `docs/architecture.md`. Conventions added: BRANCH-001 (direct commit to main), TEST-003 (tmpdir test repos disable git hooks). Bug logged: 005 (workstation pre-commit hook + tmpdir test repos). Plugin cache refreshed via `/refresh-plugin-cache` skill — all 17 l3digitalnet plugins now CURRENT. |
