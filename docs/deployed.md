# What Is Deployed

| Plugin | Version | Status |
|--------|---------|--------|
| opus-context | 1.1.0 | Released 2026-04-23; SessionStart hook rewritten to read skill body, strip YAML frontmatter, and mechanically inject JSON `hookSpecificOutput` into context window (guarantees rules present on every turn vs. optional via skill tool). SKILL.md tightened 1000 → 350 tokens; terminal banner moved to stderr. GitHub release tag: `opus-context/v1.1.0` |
| home-assistant-dev | 2.2.8 | Released 2026-05-07; dropped `WebSearch` from `VALID_AGENT_TOOLS` allowlist in `tests/test_plugin_structure.py` (no HA agent declared it; aligned with marketplace-wide migration away from built-in `WebSearch`). Prior 2.2.7: ha-integration-reviewer agent downgraded haiku. GitHub release tag: `home-assistant-dev/v2.2.8` |
| qt-suite | 0.3.1 | Released 2026-04-23; gui-tester and test-generator agents explicitly set model: sonnet (was inherit, resolved to opus on opus sessions; ~5x cost reduction per invocation). GitHub release tag: `qt-suite/v0.3.1` |
| qdev | 1.4.0 | Released 2026-05-07; all `/qdev` commands and agents support the new `tavily` MCP server (content-heavy queries collapse search→scrape; JS-rendered page extraction). brave-search + serper-search remain primary parallel search pair; `tavily_extract` replaces `WebFetch` as preferred fetch tool for docs/advisory pages. Prior 1.3.0: subagent split for grunt work. GitHub release tag: `qdev/v1.4.0` |
| repo-hygiene | 1.4.0 | Released 2026-04-23; semantic audit (Step 2) split to hygiene-semantic-auditor haiku subagent; Step 1 (seven mechanical scripts) unchanged (~15K tokens per run reduction). GitHub release tag: `repo-hygiene/v1.4.0` |
| up-docs | 0.8.1 | Released 2026-05-25; patch release bundling 8483991 (deny-guard scope-gating fix) + bacf529/cbb9c7b (bats test-harness repair). Fixed 13 environmental + scope-coverage test failures via GIT_CONFIG_GLOBAL=/dev/null + fixture + assertion thread-through. 62/62 bats (was 74/87 pre-flight) + 26/26 pytest. GitHub release tag: `up-docs/v0.8.1`. |
| release-pipeline | 2.2.0 | Released 2026-05-07 via manual bootstrap (v2.1.2 templates couldn't release v2.2.0 — chicken-and-egg). All Phase 3 flows replaced `git checkout main && git pull && git merge testing --no-ff` with `git pull --rebase origin main`; trailing `git checkout testing` removed throughout. Mode 1 renamed Quick Merge → Quick Push (no testing branch to merge). Adapts the pipeline to the direct-commit-to-`main` repo convention adopted in this session. Prior 2.1.2: README section rename. GitHub release tag: `release-pipeline/v2.2.0` |
| plugin-test-harness | 0.7.5 | Released 2026-05-07; environmental test fix — 4 tests in `test/unit/fix/{applicator,tracker}.test.ts` create tmpdir git repos with fake author email (`test@pth.test`) and ran real `git commit` operations that the workstation's global pre-commit hook rejected. Fix: `git config core.hooksPath /dev/null` in tmpdir setup so test commits don't fire workstation hooks. Contributor-agnostic; works on any developer's machine. 18/18 suites pass, 68/68 tests (was 16/18, 64/68). Plus 18 new test cases from prior 0.7.4 (convergence + gap-analyzer + registry-shape). GitHub release tag: `plugin-test-harness/v0.7.5` |

## Recently Removed (2026-05-08)

claude-sync, design-assistant, docs-manager, linux-sysadmin, python-dev — deleted from `plugins/` in commit 3b8323e (`Cleaning up old plugins.`). Marketplace count went 17 → 12. All doc layers reconciled in the same session.

## What Remains

- Monitor Tavily `search_depth=fast` vendor bug (returns empty results for queries `basic` answers correctly). Inline-noted in routing rules; revisit if Tavily fixes upstream.
- Centralize bats wrapper to `scripts/run-plugin-bats.sh` (now 9 copies of `tests/run-bats.sh` after plugin removals — was 13). TEST-002 in conventions covers the workaround pattern; centralization is optional cleanup.
- ~~Canonicalize the `core.hooksPath=/dev/null` test-fixture pattern (TEST-003 / bug 005) across every plugin's `tests/test_helper.bash`~~ **DONE 2026-05-25.** Applied to all 7 remaining bats helpers (`handoff`, `repo-hygiene`, `test-driver` recovered 19 silently-failing tests; `github-repo-manager`, `nominal`, `opus-context`, `qt-suite` prophylactic). Uses file-level `export GIT_CONFIG_GLOBAL=/dev/null` (broader than the per-test `git config core.hooksPath` form — also neutralizes `commit.gpgsign` and `tag.gpgsign`). Survey now: `grep -L GIT_CONFIG_GLOBAL plugins/*/tests/{helpers,test_helper}.bash` → empty.
